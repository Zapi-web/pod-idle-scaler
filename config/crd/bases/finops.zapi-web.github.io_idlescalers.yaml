---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.22.0
  name: idlescalers.finops.zapi-web.github.io
spec:
  group: finops.zapi-web.github.io
  names:
    kind: IdleScaler
    listKind: IdleScalerList
    plural: idlescalers
    singular: idlescaler
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: IdleScaler is the Schema for the idlescalers API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of IdleScaler
            properties:
              idleTimeout:
                default: 15m
                description: Time before reduce
                type: string
              replicas:
                default: 1
                description: Replicas to scale up to when waking up
                format: int32
                minimum: 1
                type: integer
              scaleMinimum:
                default: 0
                description: Minimal scaling number
                format: int32
                minimum: 0
                type: integer
              scaleTargetRef:
                description: Link on Deployment
                properties:
                  apiVersion:
                    description: apiVersion is the API version of the referent
                    type: string
                  kind:
                    description: 'kind is the kind of the referent; More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
                    type: string
                  name:
                    description: 'name is the name of the referent; More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names'
                    type: string
                required:
                - kind
                - name
                type: object
              trigger:
                description: Gets Activity metric from Custom/External Metrics API
                properties:
                  http:
                    description: HTTP trigger configuration
                    properties:
                      bodyPattern:
                        description: BodyPattern is a regex pattern that response
                          body must match to consider service active
                        type: string
                      expectedCode:
                        default: 200
                        description: ExpectedCode is the expected HTTP answer code
                        format: int32
                        maximum: 599
                        minimum: 100
                        type: integer
                      headers:
                        additionalProperties:
                          type: string
                        description: Headers include in the request
                        type: object
                      method:
                        default: GET
                        description: Method for HTTP request
                        enum:
                        - GET
                        - POST
                        - HEAD
                        type: string
                      timeout:
                        default: 5s
                        description: Timeout for HTTP request
                        type: string
                      tlsConfig:
                        description: TLSconfig provides TLS connection settings
                        properties:
                          caSecretRef:
                            description: CASecretRef references a Secret containing
                              the CA bundle
                            properties:
                              key:
                                description: The key of the secret to select from.  Must
                                  be a valid secret key.
                                type: string
                              name:
                                default: ""
                                description: |-
                                  Name of the referent.
                                  This field is effectively required, but due to backwards compatibility is
                                  allowed to be empty. Instances of this type with an empty value here are
                                  almost certainly wrong.
                                  More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                type: string
                              optional:
                                description: Specify whether the Secret or its key
                                  must be defined
                                type: boolean
                            required:
                            - key
                            type: object
                            x-kubernetes-map-type: atomic
                          insecureSkipVerify:
                            description: InsecureSkipVerify disable TLS certificate
                              verification
                            type: boolean
                        type: object
                      tokenSecretRef:
                        description: TokenSecretRef references a Secret key containing
                          an auth token for headers
                        properties:
                          key:
                            description: The key of the secret to select from.  Must
                              be a valid secret key.
                            type: string
                          name:
                            default: ""
                            description: |-
                              Name of the referent.
                              This field is effectively required, but due to backwards compatibility is
                              allowed to be empty. Instances of this type with an empty value here are
                              almost certainly wrong.
                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                            type: string
                          optional:
                            description: Specify whether the Secret or its key must
                              be defined
                            type: boolean
                        required:
                        - key
                        type: object
                        x-kubernetes-map-type: atomic
                      url:
                        description: URL endpoint to probe
                        pattern: ^https?://.*
                        type: string
                    required:
                    - url
                    type: object
                  promql:
                    description: PromQL trigger configuration
                    properties:
                      query:
                        description: Query is the PromQL code to execute
                        type: string
                      threshold:
                        anyOf:
                        - type: integer
                        - type: string
                        default: "0"
                        description: |-
                          Threshold defines the value above which the workload equals active state
                          Default to 0 (metric value > 0 resets the timer)
                        pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                        x-kubernetes-int-or-string: true
                      timeout:
                        default: 5s
                        description: Timeout for the query execution
                        type: string
                      tlsConfig:
                        description: TLSconfig provides TLS connection settings
                        properties:
                          caSecretRef:
                            description: CASecretRef references a Secret containing
                              the CA bundle
                            properties:
                              key:
                                description: The key of the secret to select from.  Must
                                  be a valid secret key.
                                type: string
                              name:
                                default: ""
                                description: |-
                                  Name of the referent.
                                  This field is effectively required, but due to backwards compatibility is
                                  allowed to be empty. Instances of this type with an empty value here are
                                  almost certainly wrong.
                                  More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                type: string
                              optional:
                                description: Specify whether the Secret or its key
                                  must be defined
                                type: boolean
                            required:
                            - key
                            type: object
                            x-kubernetes-map-type: atomic
                          insecureSkipVerify:
                            description: InsecureSkipVerify disable TLS certificate
                              verification
                            type: boolean
                        type: object
                      tokenSecretRef:
                        description: TokenSecretRef references a Secret key for Prometheus
                          Bearer auth
                        properties:
                          key:
                            description: The key of the secret to select from.  Must
                              be a valid secret key.
                            type: string
                          name:
                            default: ""
                            description: |-
                              Name of the referent.
                              This field is effectively required, but due to backwards compatibility is
                              allowed to be empty. Instances of this type with an empty value here are
                              almost certainly wrong.
                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                            type: string
                          optional:
                            description: Specify whether the Secret or its key must
                              be defined
                            type: boolean
                        required:
                        - key
                        type: object
                        x-kubernetes-map-type: atomic
                      url:
                        description: URL of the Prometheus/VictoriaMetrics etc.
                        pattern: ^https?://.*
                        type: string
                    required:
                    - query
                    - url
                    type: object
                  type:
                    description: Type defines the trigger mechanism
                    enum:
                    - HTTP
                    - PromQL
                    type: string
                required:
                - type
                type: object
                x-kubernetes-validations:
                - message: trigger configuration must match the specified type and
                    only one config can be provided
                  rule: (self.type == 'HTTP' && has(self.http) && !has(self.promql))
                    || (self.type == 'PromQL' && has(self.promql) && !has(self.http))
            required:
            - scaleTargetRef
            - trigger
            type: object
          status:
            description: status defines the observed state of IdleScaler
            properties:
              conditions:
                description: The status of each condition is one of True, False, or
                  Unknown.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastActivityTime:
                description: Last Activity Time
                format: date-time
                type: string
              phase:
                description: Phase provides a summary of the scaller state
                enum:
                - Active
                - Idling
                - Sleeping
                - Error
                type: string
              readyReplicas:
                description: Number of ready replicas
                format: int32
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
